export const USER_STORAGE = "@planner:user";

/** AsyncStorage legacy key (may contain @ and :). */
export const AUTH_TOKE_STORAGE = "@planner:token";

/** SecureStore key — alphanumeric, ".", "-", "_" only. */
export const AUTH_TOKEN_STORAGE = "planner.auth.token";